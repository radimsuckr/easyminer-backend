services:
  postgres:
    environment:
      POSTGRES_DB: easyminer
      POSTGRES_PASSWORD: easyminer
      POSTGRES_USER: easyminer
    image: postgres:latest
    ports:
      - 5432:5432

  redis:
    image: redis:latest
    ports:
      - 6379:6379
