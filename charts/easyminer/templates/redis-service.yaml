{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "easyminer.fullname" . }}-redis
  labels:
    {{- include "easyminer.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    {{- include "easyminer.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end }}
